# 2026-03-19

## Issue #43: Resumable bulk upsert (feat/issue-43-resumable-bulk-upsert)

Completed the implementation of checkpoint/resume support for `entity batch upsert`:

- **CLI flags**: Added `--checkpoint` and `--resume` to `BatchEntityCommands::Upsert` in `commands.rs`
- **Runner wiring**: Updated `runner.rs` to pass `checkpoint` and `resume` through to `entity_batch_upsert`
- **Checkpoint helpers in `batch.rs`**:
  - `load_checkpoint_indices(path, key)` — reads JSONL, returns `HashSet<usize>`
  - `open_checkpoint_writer(path, resume)` — append (resume) or truncate (new)
  - `write_checkpoint(writer, result)` — per-item JSONL line via `Mutex<File>`
- **Tests**: 12 tests in `batch_upsert_checkpoint_tests.rs` — all passing
- **Dev-dep**: Added `tempfile = "3"` to `fpt-domain/Cargo.toml`
- **Committed**: `1318082` on branch `feat/issue-43-resumable-bulk-upsert`

## All three issue branches pushed and PRs created

| Issue | Branch | PR |
|-------|--------|----|
| #44 (structured search consistency) | `fix/issue-44-structured-search-consistency` | [PR #53](https://github.com/loonghao/fpt-cli/pull/53) |
| #42 (entity-link filter ergonomics) | `fix/issue-42-entity-link-filter-ergonomics` | [PR #52](https://github.com/loonghao/fpt-cli/pull/52) |
| #43 (resumable bulk upsert) | `feat/issue-43-resumable-bulk-upsert` | [PR #54](https://github.com/loonghao/fpt-cli/pull/54) |

## Rebased PR #52 (fix/issue-42-entity-link-filter-ergonomics) onto origin/main

- Resolved merge conflict in `crates/fpt-domain/src/app/find.rs` — kept issue-42's entity-link validation, shorthand constants, and helper functions while incorporating issue-44's normalization fixes already on main
- Fixed clippy `too_many_arguments` warning on `entity_batch_upsert` by adding `#[allow(clippy::too_many_arguments)]`
- All clippy checks pass (zero warnings), all workspace tests pass
- Force-pushed rebased branch to origin: `217d55b`
- CI `fmt-check` failed — ran `cargo fmt --all` to fix formatting in 4 files (batch.rs, batch_upsert_checkpoint_tests.rs, entity_link_filter_tests.rs, find_params_tests.rs)
- Amended commit and force-pushed again: `55effd1`
