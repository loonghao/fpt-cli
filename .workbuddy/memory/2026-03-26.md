# 2026-03-26

## Extended REST API coverage (PR #104 - Merged)

Analyzed the full ShotGrid REST API documentation and identified 5 new endpoints not covered by fpt-cli. Created branch `feat/extend-rest-api-coverage` from `origin/main`, implemented all changes, and merged PR #104.

### New endpoints added:
1. **`hierarchy expand`** — `POST /hierarchy/expand` — Expand hierarchy tree nodes
2. **`schedule work-day-rules`** — `GET /schedule/work_day_rules` — Read work day rules
3. **`schedule work-day-rules-update`** — `PUT /schedule/work_day_rules/{id}` — Update work day rules
4. **`license get`** — `GET /license` — Read site license info
5. **`preferences custom-entity`** — `POST /preferences/custom_entity` — Enable custom entity types

### Changes across all architecture layers:
- **Transport** (`transport.rs`): 5 new trait methods + RestTransport impls
- **App**: New `license.rs`, `schedule.rs` modules; extended `hierarchy.rs`, `activity.rs`
- **Capability**: 5 new CommandSpec definitions
- **CLI**: New `ScheduleCommands`, `LicenseCommands` enums; extended `HierarchyCommands`, `PreferencesCommands`
- **Runner**: Full routing for all new endpoints
- **Tests**: 20 new tests (app command, REST transport, CLI contract)

### CI issues resolved:
- `rustfmt` formatting differences (ran `cargo fmt --all`)
- `workspace-hack` Cargo.toml drift (ran `cargo hakari generate`)

Total test count after merge: **296 tests, 0 failures**.
